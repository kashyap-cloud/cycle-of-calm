server {
    listen 80;
    absolute_redirect off;
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri $uri/ /ocd_cycle/index.html;
    }
    
    location = /ocd_cycle {
        rewrite ^/ocd_cycle$ /ocd_cycle/index.html last;
    }

    error_page 404 /ocd_cycle/index.html;
    
    location = / {
        return 301 /ocd_cycle/;
    }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
